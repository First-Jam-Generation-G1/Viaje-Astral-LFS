using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class playGanador : MonoBehaviour
{
    public AudioSource ganador;
    // Start is called before the first frame update
    void Start()
    {
        ganador.Play();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
